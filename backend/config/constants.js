const DAYS_IN_WEEK = 7;
const ONE_HOUR_SECONDS = 3600;
const MS_PER_SECOND = 1000;
const TOP_NUMBER = 5;
const SCORE_BASE = 1;
const SCORE_MORE = 2

module.exports = {
    DAYS_IN_WEEK,
    ONE_HOUR_SECONDS,
    MS_PER_SECOND,
    TOP_NUMBER,
    SCORE_BASE,
    SCORE_MORE 
}
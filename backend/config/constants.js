const DAYS_IN_WEEK = 7;
const ONE_HOUR_SECONDS = 3600;
const MS_PER_SECOND = 1000;
const TEN_HOUR_SECONDS = 36000;
const FIVE_MINUTES = 300;
const TOP_NUMBER = 5;

const WEIGHTS = {
    bio: 4.0,
    interest: 3.0,
    graph: 2.0,
    major: 1.5,
    school: 1.0,
    bonus: 1.5
}

module.exports = {
    DAYS_IN_WEEK,
    ONE_HOUR_SECONDS,
    MS_PER_SECOND,
    TEN_HOUR_SECONDS,
    FIVE_MINUTES,
    TOP_NUMBER,
    WEIGHTS 
}